# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
#CFLAGS="-O2 -pipe"
#CXXFLAGS="${CFLAGS}"
# WARNING: Changing your CHOST is not something that should be done lightly.
# Please consult http://www.gentoo.org/doc/en/change-chost.xml before changing.
CHOST="x86_64-pc-linux-gnu"
# These are the USE flags that were used in addition to what is provided by the
# profile used for building.
#USE="bindist mmx sse sse2"
PORTDIR="/usr/portage"
DISTDIR="${PORTDIR}/distfiles"
PKGDIR="${PORTDIR}/packages"

CFLAGS="-Ofast -mmmx -mssse3 -pipe -funroll-loops -flto=8 -floop-block -floop-interchange -floop-strip-mine -ftree-loop-distribution"
CXXFLAGS="${CFLAGS}"

USE="-systemd -pulseaudio -avahi -dbus -zeroconf -nls -doc -gnome-keyring -gstreamer -libav -openal -kde -gnome -qt3support -qt4 bindist ipv6 minimal jpeg gif png offensive zsh-completion custom-cflags custom-optimization pgo cpudetection threads aio smp nptl lto graphite pgo numa cxx alsa xinerama opengl opencl glamor vaapi vdpau xvmc"
CPU_FLAGS_X86="mmx mmxext sse sse2 sse3 ssse3"

EMERGE_DEFAULT_OPTS="--keep-going=y --autounmask-write=y --jobs=2"
MAKEOPTS="-j8"
PORTAGE_NICENESS=15

AR="gcc-ar"
NM="gcc-nm"
RANLIB="gcc-ranlib"

# binutils-config --linker ld.gold
